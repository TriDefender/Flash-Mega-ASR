[project]
name = "flash-mega-asr"
version = "0.1.0"
description = "Flash-attention optimized Mega-ASR: fast batched inference with LoRA routing for robust speech recognition"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [
    { name = "Flash-Mega-ASR Contributors" },
]

dependencies = [
    "qwen-asr>=0.0.6",
    "torch>=2.10",
    "torchaudio>=2.10",
    "safetensors",
    "soundfile",
    "peft",
    "rich>=13.0",
]

[project.optional-dependencies]
flash-attn = [
    "flash-attn>=2.5",
]
webui = [
    "streamlit",
    "streamlit-mic-recorder",
    "matplotlib",
]
eval = [
    "editdistance",
    "sacrebleu",
    "whisper-normalizer",
    "zhconv",
    "tqdm",
    "datasets",
]
dev = [
    "pytest",
    "ruff",
]
all = [
    "flash-mega-asr[flash-attn,webui,eval,dev]",
]

[project.scripts]
flash-mega-asr = "MegaASR.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/MegaASR"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 120
target-version = "py310"
